@model Wedding

    <h1>@Model.WedderOne & @Model.WedderTwo's Wedding!</h1>
    <h3>@Model.Date</h3>
    <h4>@Model.Address</h4>

    @foreach(Rsvp reserve in Model.Rsvps)
    {
            <p>@reserve.user.FirstName @reserve.user.LastName</p>
    }
